# Camera source configuration
# Options: "opencv" or "libcamera"
CAMERA_SOURCE=opencv

# OpenCV camera device number (default: 0)
OPENCV_DEVICE=0

# Libcamera device path (default: /dev/video0)
LIBCAMERA_DEVICE=/dev/video0
